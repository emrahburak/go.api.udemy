package main

import "fmt"

type Human struct {
	firstName string
	lastName  string
	age       int
}

func main() {
	// Nesne Öreneği oluşturma

	//v1
	// x := Human{firstName: "Emrah"}
	// fmt.Println(x.firstName)

	//v2
	x := new(Human)
	fmt.Println(x.firstName)
}
